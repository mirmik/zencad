from zencad import *
import math

#def base_nut(irad, orad, h, pitch):
#	step = orad * math.sin(pitch)

#	pseg = polysegment(points([(irad, 0, step/2), (orad, 0, step), (orad, 0, 0)]))
#	return pseg

